import React from 'react';

// input will require state -> use class

class Input extends React.Component {
  constructor(props) {
    super(props);
  }
  render() {
    return (
      <div>
        <p>y0, it's y0 Input, Drew!</p>
      </div>
    );
  }
}

export default Input;
